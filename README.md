# my-render-cron

A scheduled cron job deployed on Render.

## Deploy
1. Push to GitHub
2. Go to render.com → New → Cron Job
3. Connect repo, set schedule, deploy!

## Schedule
Default: every 5 minutes (`*/5 * * * *`)
